module.exports = {
	clusterCount: 1, // How many clusters to spawn
	shardCount: 1, // How many shards to spawn (Will be evenly spread amongst clusters)
	color: 0xEB459E, // Color for your Embeds
	ecolor: 0xFF0000,
	prefix: ',', // Your default prefix
	owners: ['737553088218529813'], // Array of USER IDs that can use owner-only commands
	mongoUser: 'MONGO DB HOST URI', // As what users you're storing data
}